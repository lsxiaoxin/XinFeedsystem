# XinFeedSystem — 简历核心亮点（Top 5）

> 写简历时优先用这 5 条，每条都能在面试中展开至少 5 分钟。

---

## 1. 分布式锁防缓存击穿（SETNX + Lua 原子释放）

**问题**：热点视频缓存失效瞬间，大量并发请求同时穿透到 MySQL，形成"惊群"。

**方案**：自研轻量分布式锁（`pkg/redislock`），SETNX 写入 UUID token，Unlock 用 Lua 脚本原子比对后删除。封装 `Do()` 方法实现 Cache-Aside 防击穿模式：
- 持锁者（1 个 goroutine）执行 DB 查询并回填缓存
- 等待者轮询缓存（每 20ms），命中后直接返回，最多等待 500ms

**效果**：同一 key 的 N 个并发 miss 只触发 1 次 DB 查询；锁 TTL=3s，持锁者崩溃后自动释放，不永久阻塞后续请求。

**代码位置**：`pkg/redislock/lock.go`，`internal/service/video_service.go::GetDetail`

---

## 2. Redis ZSet 滑动窗口热榜——彻底解决翻页重复/漏数

**问题**：热榜按 `heat DESC` 游标分页，翻第 2 页时 `heat` 已被点赞改变，导致同一视频出现两次或被跳过。

**方案**：后台 `SnapshotService` 每分钟重建一次 Redis ZSet 快照（`snapshot:popularity:v{epoch}`，Top 1000，TTL 15min），游标编码 `(epoch, offset)` 而非 `(score, id)`：
- 翻页中始终 `ZREVRANGE` 同一个 epoch 的 ZSet，榜单对用户完全冻结
- 旧 epoch 过期时透明回退到最新 epoch + offset=0，前端无感知
- `SET snapshot:popularity:current` 在 ZADD pipeline 完成后才更新，保证指针始终指向完整快照

**效果**：无论期间发生多少次点赞/评论，翻页结果严格稳定，无重复无漏数。

**代码位置**：`internal/service/snapshot_service.go`，`internal/service/feed_fetcher.go::SnapshotFetcher`

---

## 3. Token 双写 Redis + DB，中间件 O(1) 鉴权

**问题**：原方案中间件每次请求执行 `SELECT token FROM users WHERE id=?`，QPS 越高 DB 压力越大。

**方案**：登录时将 token 同时写入 Redis（`user:token:{uid}`，TTL = JWT 有效期）和 DB（source of truth）。中间件验证逻辑：
1. Redis GET → 命中且匹配 → 放行（不走 DB）
2. Redis miss → DB 查询 → 匹配则回填 Redis（TTL = JWT 剩余有效期）→ 放行
3. Redis 任意错误降级到 DB，不影响业务可用性

**效果**：稳定态请求验证完全在 Redis 完成，DB 仅在重启/缓存冷启时被访问一次（之后自动回填）。登出同时删 Redis 和清 DB token，两端一致。

**代码位置**：`internal/repository/token_cache.go`，`internal/middleware/jwt.go::validateToken`

---

## 4. 缓存三道防线的系统性实现

**背景**：引入 Redis 缓存后，需要同时防御缓存穿透、雪崩、击穿三类风险。

**三道防线**：

| 问题 | 解法 | 关键参数 |
|---|---|---|
| **穿透**（DB 无此记录反复查） | DB miss 写 `__nil__` 占位 | TTL 30s |
| **雪崩**（大批 key 同时过期） | `RandomizedTTL(base ± jitter)` 打散 | 视频 5min±30s，用户 10min±30s |
| **击穿**（热点 key 失效惊群） | 分布式锁 `Do()` 序列化回源 | 锁 TTL 3s，等待上限 500ms |

三种防御在 `pkg/cache` 和 `pkg/redislock` 中统一封装，各 Service 调用接口一致。

**代码位置**：`pkg/cache/json.go`，`pkg/redislock/lock.go`

---

## 5. 四种 Feed 策略 + 游标分页（可测试、可扩展）

**设计**：`FeedFetcher` 接口将游标 encode/decode 职责下沉到各策略，`FeedService` 完全不感知游标格式：

```go
type FeedFetcher interface {
    Type() string
    Fetch(ctx context.Context, rawCursor string, limit int) (
        videos []*entity.Video, nextCursor string, hasMore bool, err error)
}
```

四种策略各自持有游标逻辑：最新流 / 关注流用 `(created_at, id)`，热榜 / 点赞榜用 `(epoch, offset)`。新增策略只需实现接口并在 `main.go` 注册一行，不修改任何已有代码（开闭原则）。

游标分页用 `WHERE score < ? OR (score = ? AND id < ?)` 代替 `OFFSET`，第 N 页性能与第 1 页相同。

**可测试**：抽取 `videoStore` 接口，全部 fetcher 和 SnapshotService 均可通过 miniredis + stub 独立单测，无需真实 DB。

**代码位置**：`internal/service/feed_fetcher.go`，`internal/service/feed_service.go`，测试见 `internal/service/feed_fetcher_test.go`

---

## 面试高频追问 & 准备方向

| 亮点 | 最可能被追问的点 |
|---|---|
| 分布式锁 | Lua 脚本为什么能保证原子性？TTL 设多少合适？如果 loader 超时怎么办？ |
| ZSet 快照 | 为什么不直接用 DB 游标？epoch 过期时用户体验如何？快照重建失败怎么降级？ |
| Token 双写 | 写 Redis 成功但 DB 失败怎么处理？Redis 宕机后鉴权还能用吗？ |
| 缓存三防 | nil sentinel 的 TTL 为什么设 30s 而不是更长？RandomizedTTL 的 jitter 怎么取值？ |
| 策略模式 | 如果要加权重混合 Feed 怎么扩展？cursor 格式不兼容怎么做版本兼容？ |
